import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the floating Next.js dev badge in the corner -- it sits on top of the
  // playbar and shows up in every screenshot.
  devIndicators: false,
};

export default nextConfig;
